
<div class="row">
    <div class="span12 col-lg-12">
        <div class="panel panel-success">
            <div class="panel-heading">
                <h3 class="panel-title">{{ 'title_user_edit_password'|trans({}, 'SonataUserBundle') }}</h3>
            </div>
            <div class="panel-body">
				<form action="{{ path('sonata_user_change_password') }}" {{ form_enctype(form) }} method="POST" class="fos_user_change_password">
				    {{ form_widget(form) }}
				    <div>
				        <button type="submit" name="submit" class="btn btn-warning pull-right">{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}</button>
				    </div>
				</form>
            </div>
        </div>
    </div>
</div>
