{% extends 'MairieVoreppeDemandeTravauxBundle::layout.html.twig' %}

{% block main_content %}

	<div class="row">  
		<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">

			<h1>Entreprise edit</h1>

					
		    {{ form_start(edit_form) }}
		        <div class="form-group">
		            {{ form_errors(edit_form.prestataireDICT) }}
		            {{ form_widget(edit_form.prestataireDICT) }}
		        </div>
		        {{ form(edit_form) }}
		    {{ form_end(edit_form)}}

			
			<ul class="list-unstyled">
				<li>
					<a  type="button" class="btn btn-default" href="{{ path('entreprise') }}">
						<i class="fa fa-backward"></i> Retourner à la liste des entreprises
					</a>
				</li>
				<li>{{ form(delete_form) }}</li>
			</ul>
		</div>
	</div>
{% endblock %}
