{% extends 'MairieVoreppeDemandeTravauxBundle:MaitreOeuvre:layout.html.twig' %}

 {% block moa_content %}


<section class="table-responsive">
    <hr/>
    <caption>
        <h1>MOEPersonneMorale list</h1>
    </caption>
    <table class="table table-bordered table-striped table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Raison sociale</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('moepersonnemorale_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.entreprise }}</td>
                <td>                   
                    <ul class="list-unstyled list-inline">
                        <li>
                            <a class="btn btn-info" href="{{ path('moepersonnemorale_show', { 'id': entity.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a class="btn btn-success" href="{{ path('moepersonnemorale_edit', { 'id': entity.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('moepersonnemorale_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
</section>
    {% endblock %}
