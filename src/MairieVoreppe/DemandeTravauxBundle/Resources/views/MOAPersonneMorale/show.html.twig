{% extends 'MairieVoreppeDemandeTravauxBundle:MaitreOuvrage:layout.html.twig' %}

 {% block moa_content %}
    <h1>MOAPersonneMorale</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('moapersonnemorale') }}">
            Back to the list
        </a>
    </li>
    <li>
        {% if entity.personneMorale is instanceof('MairieVoreppe\\DemandeTravauxBundle\\Entity\\Entreprise') %}
            <a href="{{ path('moapersonnemorale_edit_entreprise', { 'id': entity.id }) }}">
                Edit
            </a>
        {% endif %}
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
