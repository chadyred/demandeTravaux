{% extends 'MairieVoreppeDemandeTravauxBundle:MaitreOeuvre:layout.html.twig' %}

 {% block moa_content %}

   
<section class="table-responsive">
    <hr/>
    <caption>
        <h1>MOEPersonnePhysique list</h1>
    </caption>
    <table class="table table-bordered table-striped table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Raison sociale</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('moepersonnephysique_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.civil }}</td>
                <td>                    
                    <ul class="list-unstyled list-inline">
                        <li>
                            <a class="btn btn-info" href="{{ path('moepersonnephysique_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a class="btn btn-success" href="{{ path('moepersonnephysique_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('moepersonnephysique_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
</section>
    {% endblock %}
