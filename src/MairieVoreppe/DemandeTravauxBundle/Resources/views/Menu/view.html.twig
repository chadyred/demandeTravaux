{% set route = app.request.attributes.get('route') %}
    

<div class="row">
    <div class="col-lg-12 col-sm-12 col-xs-12 col-md-12">
        <nav class="navbar navbar-fixed-top navbar-custom" role="navigation">   
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>                 
                <span class="navbar-brand visible-lg-block">
                    <a href="#">Menu <i class="fa fa-arrow-right"></i> </a>
                </span>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav navbar-left">
                    <li {{ route == "mairie_voreppe_demande_travaux_homepage" ? 'class="active"' : '' }}>
                            <a href="{{ path('mairie_voreppe_demande_travaux_homepage') }}">Accueil</a>
                    </li>
                    <li {{ route == "maitre_ouvrage" ? 'class="active"' : '' }} >
                        <a href="{{ path('maitre_ouvrage') }}">Maître d'ouvrage</a>
                    </li>
                    <li {{ route == "maitre_oeuvre" ? 'class="active"' : '' }} >
                            <a href="{{ path('maitre_oeuvre') }}">Maître d'oeuvre</a>
                    </li>
                    <li {{ route == "travaux" ? 'class="active"' : '' }} >
                            <a href="{{ path('travaux') }}">Travaux</a>
                    </li>
                    <li {{ route == "arretemodel" ? 'class="active"' : '' }} >
                            <a href="{{ path('arretemodel') }}">Arrêté</a>
                    </li>
                    <li {{ route == "recepisse" ? 'class="active"' : '' }}>
                            <a href="{{ path('recepisse') }}">Récépissé</a>
                    </li>
                    <li {{ route == "mairie" ? 'class="active"' : '' }}>
                            <a href="{{ path('mairie') }}">La Mairie</a>
                    </li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown ">							
                       <a data-toggle="dropdown" href="#" class="navbar-link">Compte<b class="caret"></b></a>
                       <ul class="dropdown-menu">
                           <li><a href="{{ path('fos_user_profile_show') }}" class="glyphicon glyphicon-user">Profile</a></li>
                           <li><a href="{{ path('admin_login')}}">Administration</a></li>
                           <li><a href="{{ path('fos_user_security_logout')}}">Déconnexion</a></li>
                       </ul>
                    </li>
                </ul>
            </div>
        </nav>
    </div>
</div>
